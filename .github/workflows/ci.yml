name: Build and Push Docker Image

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ bk77811 }}
          password: ${{ @Backjump3.0 }}

      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          push: true
          tags: ${{ bk77811 }}/flask-k8s-app:latest




# name: Build and Push Docker Image

# on:
#   push:
#     branches: [ main ]

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       # Checkout repo
#       - uses: actions/checkout@v4

#       # Set up QEMU for cross-platform builds (optional but good practice)
#       - name: Set up QEMU
#         uses: docker/setup-qemu-action@v2

#       # Set up Docker Buildx (extended build capabilities)
#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v2

#       # Login to Docker Hub
#       - name: Login to DockerHub
#         uses: docker/login-action@v2
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       # Build and push Docker image
#       - name: Build and push
#         uses: docker/build-push-action@v4
#         with:
#           push: true
#           tags: ${{ secrets.DOCKERHUB_USERNAME }}/flask-k8s-app:latest
